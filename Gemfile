# frozen_string_literal: true
source "https://rubygems.org"

gem "jekyll", "3.3.1"
gem "rails"
gem "rake"                  # Ruby's version of `make`
gem "foreman"               # Managing Procfile-based things.  For Heroku dev.
gem "puma"                  # The actually web server we will use.
gem "rack-contrib"          # Middleware
gem "jekyll-paginate"       # Pagination
